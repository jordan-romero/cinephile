



<div class="form-group" style="width :50em;">
    <%= form_with model: movie, local:true do |form| %>
    
    <div class= "text-weight-bold text-dark"><%= form.label :title %></div>
    <span> <%= form.text_field :title, class: "form-control" %> </span>
  </div>  
   <div class="form-group" style="width :50em;">
        <%= form.label :genre %> 
        <%= form.text_field :genre, class: "form-control" %>
    </div>
    <div class="form-group" style="width :50em;">
   
        <%= form.label :runtime %> 
        <%= form.text_field :runtime, type: 'number', class: "form-control" %>
    </div>

    <div class="form-group" style="width :50em;"> 
        <%= form.label :release_date %> 
        <%= form.text_field :release_date, type: 'date', class: "form-control" %>
    </div>
   <div class="form-group" style="width :50em;"><%= form.collection_select :list_ids, List.all, :id, :name %> 
   </div>
   <h1> Add your favorite Quote: </h1>
        <%= form.fields_for :quotes do |quote| %>
            <%= quote.label :text %>
            <%= quote.text_field :text%>

            <%= quote.label :character %>
            <%= quote.text_field :character %>
        <% end %>

   <%= form.submit %>

 


 <% end %>
