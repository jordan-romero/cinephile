<h1 class="display-4 text-primary">I am the Index</h1>

<%= text_field_tag 'search', nil, placeholder: 'Search Movies' %>
<%= submit_tag ("Search"), class: 'btn btn-outline-info ml-3' %>

<% @movies.each do |movie|%>
<%= link_to movie.title, movie_path(movie) %>
<% end %>

<%= link_to "Add a Movie to you Collection", new_movie_path%>